procs:
  mcp-server:
    cwd: apps/coeus-mcp
    shell: pnpm dev
  mcp-inspector:
    cwd: .
    shell: npx @modelcontextprotocol/inspector --mcp-url=http://localhost:3000
  ngrok:
    cwd: .
    shell: ngrok http --url=neat-perfectly-buck.ngrok-free.app 3000

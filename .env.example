# Zep Cloud Configuration
# Required for connecting to the Zep Cloud service.
# You can get an API key from the Zep Cloud dashboard.
ZEP_API_KEY=your_zep_api_key_here

# Optional: If you are using a self-hosted or custom Zep instance, specify the URL here.
# ZEP_API_URL=https://api.getzep.com

# Operational Database Configuration
# Connection string for the local operational database.
# For development, an in-memory SQLite database is often sufficient.
DATABASE_URL=sqlite:./data/coeus-dev.db

# Application Configuration
# LOG_LEVEL can be one of: error, warn, info, debug, trace
LOG_LEVEL=debug

# Optional: If you want to use OpenAI as a fallback for embeddings
# OPENAI_API_KEY=your_openai_key_here
